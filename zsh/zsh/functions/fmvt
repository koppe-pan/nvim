# Move multiple files to that directory with FZF
fmvt() {
    local obj
    obj=$(find ${10:-.} -path '*/\.*' -prune \
                    -o -type f -print 2> /dev/null | fzf +m) &&
    mv "$obj" "$1"
}
