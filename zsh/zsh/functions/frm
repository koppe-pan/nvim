# Delete multiple files with FZF
frm() {
    local obj
    obj=$(find ${1:-.} -path '*/\.*' -prune \
                    -o -type f -print 2> /dev/null | fzf -m) &&
    for f in "$obj"; do
      rm "$f"
    done
}
